Each feature is organized into its own Redux slice to keep state and logic clearly separated. The selectors folder contains custom selectors that simplify reuse of slice logic and help reduce duplication across the application.

This repository includes only a subset of slices for demonstration purposes.
